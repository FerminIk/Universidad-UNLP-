package punto3;

import ejercicio1.ArbolBinario;
import practica2.ListaEnlazadaGenerica;
import java.util.Scanner;

public class ContadorArbol<T> {
	private ArbolBinario<T> raiz;

	public ContadorArbol(ArbolBinario<T> raiz) {
		super();
		this.raiz = raiz;
	}

	public ContadorArbol() {
		super();
	}

	public ArbolBinario<T> getRaiz() {
		return raiz;
	}

	public void setRaiz(ArbolBinario<T> raiz) {
		this.raiz = raiz;
	}
	
	private ListaEnlazadaGenerica<T> InOrden(ArbolBinario<T> a, ListaEnlazadaGenerica aux) {
		if (!a.esVacio()) {
			T dato =a.getDato();
			if (a.tieneHijoIzquierdo()) {
				
			}
			if (a.tieneHijoDerecho()) {
				
			}
			
		}
		return aux;
	}
	
	private ListaEnlazadaGenerica<T> PreOrden(ArbolBinario a) {
		
		return null;
	}
	
	public ListaEnlazadaGenerica<T> numerosPares(ArbolBinario a) {
		ListaEnlazadaGenerica<T> aux = null;
		System.out.println("1. se ejecuta el metodo en InOrden");
		System.out.println("2. se ejecuta el metodo en PreOrden");
		Scanner s = new Scanner(System.in);
		int num = s.nextInt();
		if (a.esVacio()) {
			return null;
		}else {
			while (true) {
				if (num == 1) {
					return InOrden(raiz,aux);
				} else if (num == 2) {
					return PreOrden(raiz,aux);
				}
				System.out.println("numero no valido, por favor introducelo nuevamente");
				num = s.nextInt();
			}
		}
	}
}
